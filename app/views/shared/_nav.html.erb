<nav class="navbar p-3 mb-2 bg-secondary">
  <a class="navbar-brand fw-bold " href="/">Address Book</a>
    <ul class="navbar-nav">
      <li class="nav-item">
        <% if Current.user %>
          <%= button_to "Sign Out", logout_path, method: :delete, class: "", class: "btn btn-warning fw-bold", id: "session-button" %>
        <% else %>
          <%= link_to "Login", sign_in_path, class: "nav-link", remote: true, class: "btn btn-warning fw-bold" %>
        <% end %>
      </li>
    </ul>
</nav>