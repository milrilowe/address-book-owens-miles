<br/>
<%
=begin%>
 <nav class="navbar bg-body-tertiary">
  <div class="container-fluid">
<%
=end%>
    <a class="navbar-brand">People</a>
    <%
=begin%>
 <div class="d-flex">
      <input class="form-control me-2" id="filter-people" type="text" placeholder="Search" aria-label="Search">
    </div>
  </div>
</nav>
<%
=end%>

<ol>
  <% prevPerson = nil %>
  <% @people.each do |person| %>
    <% if prevPerson == nil %>
      <hr> <%= person.last_name.chr.upcase %> <hr>
    <% end %>
    <% if prevPerson && prevPerson.last_name.chr != person.last_name.chr %>
      <hr> <%= person.last_name.chr.upcase %> <hr>
    <% end %>
    <li>
      <%= render person %>
    </li>
    <% prevPerson = person %>
  <% end %>
</ol>

<%= link_to "New Person", new_person_path %>